name: Taboor CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate-structure:
    name: Validate Project Structure
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Validate critical files
      run: |
        echo "=== PROJECT STRUCTURE VALIDATION ==="
        
        # Critical files that must exist
        critical_files=(
          "frontend/index.html"
          "frontend/Main.js" 
          "frontend/style.css"
          "backend/Nootbook.ipynb"
          "backend/queue_data.csv"
          "backend/server.js"
          "database/taboor.db"
          "database/schema.sql"
        )
        
        all_files_exist=true
        for file in "${critical_files[@]}"; do
          if [ -f "$file" ]; then
            echo "✅ $file exists"
          else
            echo "❌ MISSING: $file"
            all_files_exist=false
          fi
        done
        
        if [ "$all_files_exist" = false ]; then
          echo "🚨 Critical files missing - failing CI"
          exit 1
        fi
        
        echo "✅ All critical files present"

  validate-syntax:
    name: Validate Code Syntax
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Validate Python syntax
      run: |
        echo "=== PYTHON SYNTAX VALIDATION ==="
        
        # Find all Python files
        python_files=$(find . -name "*.py" -type f)
        
        if [ -z "$python_files" ]; then
          echo "ℹ️ No Python files found"
          exit 0
        fi
        
        echo "Found Python files:"
        echo "$python_files"
        
        # Check each Python file for syntax errors
        error_found=false
        for file in $python_files; do
          if ! python -m py_compile "$file"; then
            echo "❌ SYNTAX ERROR in: $file"
            error_found=true
          else
            echo "✅ $file - Syntax OK"
          fi
        done
        
        if [ "$error_found" = true ]; then
          echo "🚨 Python syntax errors found - failing CI"
          exit 1
        fi
        
        echo "✅ All Python files have valid syntax"

    - name: Validate JavaScript syntax
      run: |
        echo "=== JAVASCRIPT SYNTAX VALIDATION ==="
        
        # Find all JavaScript files
        js_files=$(find . -name "*.js" -type f)
        
        if [ -z "$js_files" ]; then
          echo "ℹ️ No JavaScript files found"
          exit 0
        fi
        
        echo "Found JavaScript files:"
        echo "$js_files"
        
        # Check each JavaScript file for syntax errors
        error_found=false
        for file in $js_files; do
          if ! node -c "$file"; then
            echo "❌ SYNTAX ERROR in: $file"
            error_found=true
          else
            echo "✅ $file - Syntax OK"
          fi
        done
        
        if [ "$error_found" = true ]; then
          echo "🚨 JavaScript syntax errors found - failing CI"
          exit 1
        fi
        
        echo "✅ All JavaScript files have valid syntax"

  run-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    needs: [validate-structure, validate-syntax]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: Install Python dependencies
      run: |
        pip install pytest
    
    - name: Run Python tests
      run: |
        echo "=== PYTHON UNIT TESTS ==="
        
        python_test_files=$(find . -name "test_*.py" -type f)
        
        if [ -z "$python_test_files" ]; then
          echo "ℹ️ No Python test files found"
          exit 0
        fi
        
        echo "Running Python tests:"
        echo "$python_test_files"
        
        # Run pytest - this will FAIL CI if any test fails
        python -m pytest -v --tb=short
        
        echo "✅ All Python tests passed"

    - name: Run JavaScript tests
      run: |
        echo "=== JAVASCRIPT UNIT TESTS ==="
        
        js_test_files=$(find . -name "test_*.js" -type f)
        
        if [ -z "$js_test_files" ]; then
          echo "ℹ️ No JavaScript test files found"
          exit 0
        fi
        
        echo "Running JavaScript tests:"
        echo "$js_test_files"
        
        # Run each JavaScript test and capture failures
        any_test_failed=false
        for test_file in $js_test_files; do
          echo "Running: $test_file"
          if node "$test_file"; then
            echo "✅ $test_file - PASSED"
          else
            echo "❌ $test_file - FAILED"
            any_test_failed=true
          fi
        done
        
        if [ "$any_test_failed" = true ]; then
          echo "🚨 JavaScript tests failed - failing CI"
          exit 1
        fi
        
        echo "✅ All JavaScript tests passed"

  final-report:
    name: Final CI Report
    runs-on: ubuntu-latest
    needs: [validate-structure, validate-syntax, run-tests]
    if: always()
    
    steps:
    - name: Generate CI Report
      run: |
        echo " "
        echo "=========================================="
        echo "🚀 TABOOR CI PIPELINE - COMPLETE"
        echo "=========================================="
        echo " "
        echo "📊 EXECUTION SUMMARY:"
        echo "├── Project Structure: ${{ needs.validate-structure.result }}"
        echo "├── Code Syntax: ${{ needs.validate-syntax.result }}"
        echo "└── Unit Tests: ${{ needs.run-tests.result }}"
        echo " "
        
        if [[ "${{ needs.validate-structure.result }}" == "success" && 
              "${{ needs.validate-syntax.result }}" == "success" && 
              "${{ needs.run-tests.result }}" == "success" ]]; then
          echo "🎉 ALL CHECKS PASSED!"
          echo "💡 Your code is production-ready."
        else
          echo "⚠️  SOME CHECKS FAILED"
          echo "🔧 Please fix the issues above."
        fi
        
        echo " "
        echo "=========================================="